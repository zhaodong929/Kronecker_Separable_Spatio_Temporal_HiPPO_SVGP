[project]
name = "stvgp-kronecker-standalone"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "matplotlib",
    "scipy",
    "pytest",
]

[tool.setuptools.packages.find]
include = ["stvgp_kronecker*"]
